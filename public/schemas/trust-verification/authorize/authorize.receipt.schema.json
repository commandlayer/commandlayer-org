{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.commandlayer.org/schemas/v1.0.0/trust/authorize/authorize.receipt.schema.json",
  "title": "CLAS Trust Authorize Receipt",
  "description": "A signed receipt proving that an authorization decision was made.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "family",
    "verb",
    "receipt_id",
    "request",
    "decision",
    "ts",
    "metadata"
  ],
  "properties": {
    "version": {
      "const": "1.0.0"
    },
    "family": {
      "const": "trust"
    },
    "verb": {
      "const": "authorize"
    },
    "receipt_id": {
      "type": "string",
      "minLength": 1
    },
    "request": {
      "$ref": "./authorize.request.schema.json"
    },
    "decision": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "status"
      ],
      "properties": {
        "status": {
          "type": "string",
          "enum": [
            "authorized",
            "denied",
            "conditional",
            "expired"
          ]
        },
        "reason": {
          "type": "string"
        },
        "expires_at": {
          "type": "string",
          "format": "date-time"
        },
        "policy_id": {
          "type": "string"
        }
      }
    },
    "ts": {
      "type": "string",
      "format": "date-time"
    },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "proof"
      ],
      "properties": {
        "proof": {
          "$ref": "../_shared/proof.schema.json"
        },
        "trace": {
          "$ref": "../_shared/trace.schema.json"
        }
      }
    },
    "chain_root": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional receipt chain root. Genesis receipts set this to sha256:<genesis_anchor_hash>, where the anchor hash excludes the derived chain_root field to avoid circular hashing; action/example receipts may use null until full receipt storage is available.",
      "minLength": 1
    },
    "previous_receipt_hash": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional hash of the previous receipt in the chain. May be null until full receipt storage is available.",
      "minLength": 1
    },
    "chain_index": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Optional zero-based index in the receipt chain. Genesis receipts use 0; action/example receipts may use null until full receipt storage is available.",
      "minimum": 0
    },
    "parent_receipt_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional parent receipt identifier supplied by the caller when a receipt is linked to an earlier receipt. May be null until full receipt storage is available.",
      "minLength": 1
    }
  }
}
