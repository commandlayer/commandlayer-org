## Structure

- `public/` – All static pages (index.html, docs.html, demo.html, composer.html, etc.)
- `api/` – Vercel serverless functions (e.g. /api/commons-flow)
- Root – package.json, config, CI, etc.
 
 
