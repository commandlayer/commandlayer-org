{
  "name": "commandlayer-webhook-auto-verify-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Automatic webhook receipt verification example for CommandLayer canonical receipts.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check scripts/generate-samples.mjs",
    "generate:samples": "node scripts/generate-samples.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
